<UserControl x:Class="SzymaniakDlugosz.WindGear.UI.Views.ProductEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="ComboBox">
             <Setter Property="Margin" Value="0,5,0,10"/>
             <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="CheckBox">
             <Setter Property="Margin" Value="0,5,0,10"/>
             <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>
    <ScrollViewer>
    <Grid Margin="20" MaxWidth="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Edit Product" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="1" Text="Model"/>
        <TextBox Grid.Row="2" Text="{Binding Model, UpdateSourceTrigger=PropertyChanged}"/>
        
        <TextBlock Grid.Row="3" Text="Manufacturer"/>
        <ComboBox Grid.Row="4" ItemsSource="{Binding AvailableManufacturers}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding ManufacturerId}"/>
        
        <TextBlock Grid.Row="5" Text="Area (m²)"/>
        <TextBox Grid.Row="6" Text="{Binding Area, UpdateSourceTrigger=PropertyChanged}"/>
        
        <TextBlock Grid.Row="7" Text="Material"/>
        <TextBox Grid.Row="8" Text="{Binding Material, UpdateSourceTrigger=PropertyChanged}"/>
        
        <TextBlock Grid.Row="9" Text="Sail Type"/>
        <ComboBox Grid.Row="10" ItemsSource="{Binding SailTypes}" SelectedItem="{Binding Type}"/>
        
        <CheckBox Grid.Row="11" Content="Is Camber?" IsChecked="{Binding IsCamber}"/>
        
        <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" Padding="20,5" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="20,5"/>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</UserControl>
