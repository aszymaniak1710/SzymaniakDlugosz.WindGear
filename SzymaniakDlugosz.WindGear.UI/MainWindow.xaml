<Window x:Class="SzymaniakDlugosz.WindGear.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SzymaniakDlugosz.WindGear.UI.ViewModels"
        xmlns:views="clr-namespace:SzymaniakDlugosz.WindGear.UI.Views"
        mc:Ignorable="d"
        Title="WindGear Catalog" Height="600" Width="900" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ManufacturerListViewModel}">
            <views:ManufacturerListView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ManufacturerEditViewModel}">
            <views:ManufacturerEditView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProductListViewModel}">
            <views:ProductListView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProductEditViewModel}">
            <views:ProductEditView/>
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Navigation Bar -->
        <Border Background="#333" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="WindGear" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <Button Content="Manufacturers" Command="{Binding ShowManufacturersCommand}" Padding="10,5" Margin="5"/>
                <Button Content="Products" Command="{Binding ShowProductsCommand}" Padding="10,5" Margin="5"/>
            </StackPanel>
        </Border>
        
        <!-- Content Area -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>
